const layout = ({ children }) => {
      return (
            <main>
                  header
                  {children}
                  footer
            </main>
      );
};

export default layout;